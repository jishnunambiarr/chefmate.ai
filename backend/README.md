# ChefMate Backend

FastAPI backend for ChefMate.

## Setup

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

## Run

```bash
python main.py
```

API: `http://localhost:8000`
Docs: `http://localhost:8000/docs`
